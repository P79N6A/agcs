package ${bussiPackage}.dao.${entityPackage}.impl;

import ${bussiPackage}.dao.${entityPackage}.${entityName}DaoI;
import org.springframework.stereotype.Repository;

@Repository
public class ${entityName}DaoImpl implements ${entityName}DaoI {
	
}
